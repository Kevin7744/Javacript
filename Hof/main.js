function sayHi(message) {
    return 'Said... ' + message;
}

// Function expression. Setting a function equal to a variable
const sayHi = function(message) {
    return 'Said...' + message;
}

// Note -> Declarations for Global code
//      -> Expressions for local code

  